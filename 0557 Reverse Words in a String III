class Solution(object):
    def reverseWords(self, s):
        """
        :type s: str
        :rtype: str
        """
        res=[]
        s=s.split()
        for i in s:
            i=i[::-1]
            res.append(i)
        return " ".join(res)
