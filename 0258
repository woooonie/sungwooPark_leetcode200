class Solution(object):
    def addDigits(self, num):
        while(len(str(num))>1):
            temp=0
            for x in str(num):
                temp+=int(x)
            
            num=temp
            if len(str(temp))==1:
                break

        return(num)
