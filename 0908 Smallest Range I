class Solution(object):
    def smallestRangeI(self, nums, k):
        ma=max(nums)
        mi=min(nums)
        d=ma-mi
        if d<=2*k:
            return 0
        else:
            return d-2*k    
        
