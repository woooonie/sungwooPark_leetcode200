class Solution(object):
    def countSegments(self, s):
        """
        :type s: str
        :rtype: int
        """
        count=0
        l=s.split(" ")
        for i in l:
            if i==" " or i=="":
                continue
            else:
                count+=1
        return count
